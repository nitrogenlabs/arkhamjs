{
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "lerna run build",
    "clean": "lex clean && lerna run clean",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "update": "lex update -i"
  },
  "devDependencies": {
    "lerna": "^3.3.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
