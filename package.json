{
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "lerna run build",
    "clean": "lex clean && lerna run clean",
    "lint": "lerna run lint",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^2.11.0"
  },
  "workspaces": [
    "packages/*"
  ]
}